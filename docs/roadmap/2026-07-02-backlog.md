# Platform Backlog — Complete Pending Work

**Date:** 2026-07-02 (audit of `master` @ `162f722`)
**Companion:** [`2026-07-02-platform-roadmap.md`](./2026-07-02-platform-roadmap.md) — sequences these items into milestones.

No literal `TODO`/`FIXME`/`HACK` markers exist in the codebase — pending work lives in the findings doc (`docs/superpowers/notes/2026-06-17-des-live-validation-findings.md`) and in design-vs-implementation gaps. Each item below records why it exists, why it matters, dependencies, complexity, and architectural impact.

---

## A. Framework — bugs / open issues

### A1. Hydration-timeout race in search/cart specs — **OPEN, fix prescribed** *(→ M1)*
- **What:** `search-plp-pdp.spec` / `add-to-cart.spec` rely on Playwright's default 5 s expect timeout while the `/es/q/{term}` results grid measurably takes ~5 s to hydrate (0 `listitem`s at +3 s → 66 at +5 s). Passes alone, fails under load.
- **Why it exists:** DES is a Vue/shadow-DOM storefront with heavy hydration lag; three earlier *global* timeout tweaks missed because the race is in specific waits.
- **Fix (findings §7):** explicit 15–20 s timeouts on the specific `expect.poll` / `toHaveURL` / `toBeVisible` calls — not global inflation.
- **Deps:** none. **Complexity:** trivial. **Impact:** a reliably green suite is the precondition for every agentic phase. **Value:** high.

### A2. DES pre-prod error pages surface as spec failures
- **What:** occasional genuine "OH NO... ESTO ES UN ERROR" pages after card clicks (pre-prod backend/stock instability).
- **Fix direction:** small retry-on-navigation-failure helper in `src/support/`, treated as environment noise — **only if** A1 alone proves insufficient in observed runs. Not speculative.
- **Deps:** A1 landed first. **Complexity:** small. **Impact:** isolates environment noise from real regressions (also feeds the future Failure Analyzer's noise model).

### A3. `SearchBar.search()` uses a `waitForTimeout` polling loop
- **What:** deviates from the "web-first assertions only" rule; live-justified (hover-revealed trigger + hydration lag + historical tour overlay).
- **Fix direction:** refactor to an `expect.poll`-style deadline once the suite is stably green. **Complexity:** small. **Priority:** low — do not touch while flakiness is open.

### A4. `MiniCart` naming debt
- **What:** DES has no mini-cart drawer; the component roots at the cart page's "Cesta (N)" tab. Name misleads future generators/readers.
- **Fix direction:** rename to `CartSummary`/`CartTab` when next touched (Builder Engine needs honest names). **Complexity:** trivial. **Priority:** cosmetic.

---

## B. Explorer — design-vs-implementation gaps & live readiness

### B5. Shadow-DOM-blind extraction — **CRITICAL, gate to Phase 1** *(→ M2)*
- **What:** `explorer/extract/fromPage.ts` feeds the analyzer `page.content()` (light DOM only); DES renders through `bds-` web components, so most interactive content is invisible to it (findings §8).
- **Fix direction:** accessibility-tree-driven extraction (`ariaSnapshot()` / Playwright role-based enumeration) as the live path, keeping the linkedom analyzer for offline unit tests.
- **Deps:** none (design work first — own spec cycle). **Complexity:** medium. **Impact:** unblocks all Knowledge phases; without it the platform has no real map of DES.

### B6. Crawler never suppresses the onboarding tour *(→ M2, bundled with B5)*
- **What:** `crawlSession` calls `acceptConsent` but bypasses `BasePage.goto()`, so `suppressOnboardingTour`'s `bsk_onboarding` cookie is never seeded — a live crawl will hit the driver.js overlay.
- **Fix:** call `suppressOnboardingTour(page)` before the first navigation in the crawler. **Complexity:** trivial.

### B7. No canonical map exists *(→ M2 exit criterion)*
- **What:** `coverage/functional-map.json` has never been produced; the versioned-map + diff value proposition is unproven end-to-end.
- **Fix:** first live crawl after B5/B6 → review → commit. **Deps:** B5, B6, VPN access.

### B8. Flows are single-page pseudo-flows *(→ M4)*
- **What:** `MapFlow.steps` always holds exactly one page id; the Coverage/Planning agents need real multi-step journeys (Home→Search→PLP→PDP→Cart).
- **Fix direction:** flow synthesis from crawl link-graph + page types. **Deps:** B7 (a real map to synthesize from). **Complexity:** medium. **Impact:** the core Phase-2→3 knowledge upgrade.

### B9. Recorded design deviations (accept or fix deliberately)
| Deviation | Design said | Implementation does | Disposition |
|---|---|---|---|
| Crawl time budget | bounded also by time | only maxPages/maxDepth/politeness | Add with B5 rework (cheap safety) |
| Nav menus/overlays | opened during crawl | link-following only | Revisit in M4 (interaction-based discovery) |
| Crawl errors | `errors[]` section in artifact | fake pages titled `ERROR: ...` — pollutes map & diffs | Fix in M2 |
| LLM output | tool-use structured JSON | plain-text JSON parse | Acceptable; revisit if parse failures appear |
| Live LLM smoke test | thin deferred test | none | With M2 live work |
| `explorer/__fixtures__/` captured DOMs | representative saved fixtures | inline generic HTML in tests | Capture real DOMs during M2 live crawl |
| `normalizePath` lowercases paths | — | may break case-sensitive routes | Verify against live URLs in M2 |

### B10. `.env.example` missing Explorer variables
- **What:** `EXPLORER_MODE`, `EXPLORER_MAX_PAGES`, `EXPLORER_ALLOW_PROD`, `ANTHROPIC_API_KEY` are undocumented.
- **Complexity:** trivial. Bundle with M2.

---

## C. CI/CD

### C11. e2e runner reachability unverified
- **What:** the `e2e` GitLab job needs runners that reach `*.inditex.grp`; noted in `.gitlab-ci.yml` comments, never confirmed. May need a self-hosted runner + `tags:`.
- **Deps:** GitLab project access. **Complexity:** small (config), unknown (infra).

### C12. Explorer diff gate not wired into CI *(→ M3)*
- **What:** `pnpm explore --diff --fail-on-new` was designed as the CI new-flow detector. **Deps:** B5–B7, C11.

### C13. Flaky-result tagging for the future Reporting Agent
- **What:** foundation spec §6 wants CI retries surfaced (not hidden); `reports/results.json` exists but nothing consumes it. **Defer** to the Reporting phase — the seam is already in place.

---

## D. Test coverage

### D14. Suite growth policy
- Only the reference trio exists, **by design**. New tests should come from the functional map via the Test Generator (Phase 4+), not manual accretion. Resist hand-writing more specs beyond what pattern-proving requires.

### D15. Checkout/payment flows untested
- Allowed on DES (`checkoutAllowed: true`), never on prod. Highest-risk flows to automate against unstable pre-prod — defer until after M1 stability and M2 knowledge land.
