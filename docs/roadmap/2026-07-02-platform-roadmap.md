# Agentic QA Platform — Roadmap & Module Evolution

**Date:** 2026-07-02
**Status:** COMPLETE (2026-07-14). No milestone or phase gate can land. Maintained as the **chronological history log**; the live prioritized pending list is the backlog's own resume section.
**Sources of truth:** `CLAUDE.md`, `docs/superpowers/specs/2026-06-17-qa-foundation-design.md`, `docs/superpowers/specs/2026-06-17-explorer-agent-design.md`, `docs/superpowers/notes/2026-06-17-des-live-validation-findings.md` (+ its archive `…-findings-archive.md`, holding §1, §5, §8–§23, §25, §30 and §32 across three tranches, 2026-08-06, 2026-08-16 and 2026-08-17 — not auto-loaded)
**Companion:** [`2026-07-02-backlog.md`](./2026-07-02-backlog.md) — the complete pending-work backlog this roadmap sequences.

---

## North Star

Every commit must increase one of these four capabilities:

- **Knowledge** → the platform understands the application better.
- **Reasoning** → the platform makes better QA decisions.
- **Autonomy** → the platform requires less human intervention.
- **Engineering Excellence** → the codebase becomes more robust, maintainable and extensible.

If a task does not improve at least one of these capabilities, question whether it should be implemented.

---

## Where a fresh session resumes (chronological history log — latest entries at the bottom; the backlog doc's resume section holds the live prioritized pending list)

**M9 is done — the Builder now generates interaction specs from `interactions[]`, and B16 is closed.** `builder/select.ts` gained `selectInteractionJourneys`/`unsatisfiedMustCapture` (map-only selection, must-capture patterns shared with the crawler's config); `TemplateGenerator.generateInteraction` emits an open→verify→close spec mirroring `ProductPage`'s act→verify→retry idiom; the CLI wires both alongside navigation-journey generation. Same milestone closed **B16**: `loadedSignalFor` now excludes a testId hint from the loaded-signal tier when it repeats among the leaf page's own elements (deprioritize, not exclude). No schema change (still 1.5). Design: `docs/superpowers/specs/2026-07-06-m9-interaction-spec-generation-design.md`. Plan: `docs/superpowers/plans/2026-07-06-m9-interaction-spec-generation.md`.

**Live-validated 2026-07-06, subagent-driven execution (worktree `feat/m9-interaction-spec-generation`):** all 5 offline tasks reviewed clean (one Important-but-plan-mandated duplication finding in Task 4 was fixed on the spot at Jorge's direction — a dedupe refactor, not a scope change). Live run found and fixed a genuine new bug during validation (not blind-patched — root-caused via isolated reproduction): DES keeps a permanently-mounted nav-menu dialog in the DOM on every page, so the design's original `getByRole('dialog')` overlay-open signal hit a strict-mode violation once a real overlay also opened. Fixed with a baseline dialog-count diff (mirrors M8's own crawler diffing idiom). After the fix: `pnpm test:generated` 5/5 no retries (the interaction spec opens/verifies/closes the Tallas dialog live); B16 confirmed fixed live (the exact page that failed in M8b's no-regression check now picks a page-specific carousel-control signal, not the repeated wishlist testId); `pnpm test` 3/4 (A5 confirmed, unrelated, pre-existing); `pnpm plan --update` unchanged (same `/`-rooted-flows cause as §9/§12/§13/§15/§16). Findings doc §17.

**Same day, later — a full codebase architecture/quality audit ran** (Fable 5's last document-phase task before its retirement from this workflow; see CLAUDE.md's "Model routing policy", added 2026-07-06, which replaces it with Opus 4.8 for all future spec/plan/doc cycles). Diagnostic only, no code changes: `docs/superpowers/notes/2026-07-06-fable5-final-audit.md`. Two structural findings were promoted to numbered backlog items — **B17** (root-caused the M9-observed 830+ duplicate `MapElement.id` collisions: no occurrence discriminator in `makeId`, 32% of the element table is redundant, 127 duplicate instances actually diverge in their hints) and **F18** (a new backlog section, Planner: coverage matching has been structurally 0/N since M7b — seven consecutive sessions attributed this to crawl-order variability, but it's a deterministic seed/match incompatibility, not noise). Ten smaller findings (differ blind to `interactions[]`, redirect-duplicate pages paying full extraction cost before being discarded, the offline DOM extractor missing `data-qa-anchor`, the act→verify→retry idiom hand-rolled seven times, and others) are tracked in the audit doc itself with a proposed sequencing, not filed as individual backlog items yet.

**A5 is done (2026-07-12)** — `SearchResultsPage.firstProduct()` now selects by capability (standard quick-add button present, `Personalizable` badge absent), live-validated 5/5 against DES. See backlog §A and findings doc §18 for full detail.

**A6 is done (2026-07-12, same day)** — `LoginPage.login()` matches DES's current `/es/logon.html` flow again (no obsolete interstitial click); the full serialized `pnpm test` suite completes 4/4 end-to-end. See backlog §A and findings doc §19.

**The audit's "hygiene" grouping is done (2026-07-12, same day)** — F2 (differ now diffs `interactions[]`), F9 (Builder detects a stale `proposals.json`), F6 (offline extractor no longer misses `data-qa-anchor`), F11 (extraction truncation now recorded, schema 1.5→1.6, additive), F12 (`--from-report` closes the report/map shape footgun). See audit doc §3.1.

**F18 is done (2026-07-13)** — coverage matching restored: `/` dropped from the crawler's `SEEDS` plus the coupled F4 chain-truncation fix (commit `304c35e`), a live re-crawl to a 155-page map with no `/` root (commit `e02acc8`), and `pnpm plan --update` now covering 5/155 flows with `coveredBy` naming all three real manual specs — the evidence→map linkage working live for the first time since M7b. See backlog §F and findings doc §20.

**B17 is done (2026-07-13)** — `MapElement.id` collisions eliminated: extraction-time content dedup with a new `MapElement.count` field (`explorer/extract/dedup.ts`, both extraction paths) plus occurrence-discriminated ids in `buildMap`, schema 1.6 → 1.7, three commits (`c17bdcc`, `8a5cabf`+`60e7c61`, `0e4057f` — the last closing audit finding F7). Live re-crawl (commit `b8dfbdf`): 165-page map, 4,222 element rows (down from 4,809), **4,222/4,222 unique ids — zero duplicates**, 484 rows carry `count > 1`; `pnpm plan --update` 5/165 covered (no coverage regression), all suites green. See backlog §B and findings doc §21. **With B17 done, both ⚠ schema/contract-affecting audit items (B17 = F1, F18 = F5) are now closed.**

**F3 is done (2026-07-14)** — the audit's Order-2 item. The crawler now dedups the resolved path *early* (right after `acceptConsent`, before the settle wait + aria extraction + `enrichTestIds`), via a pure unit-tested `isDuplicateResolution` helper, with the existing late check kept as a safeguard for the rare URL-changed-during-settle case. Direct-TDD, output-identical, no live re-crawl. See audit doc §2/§3 F3.

**Phase 6 is done, v1 (2026-07-14, same day)** — the Risk Analysis Agent, implemented autonomously under Jorge's /goal directive (full cycle on Fable 5 per his explicit instruction — see decision log D1). New `analyzer/` sub-project (`pnpm analyze`): Failure Analyzer (classifies `reports/results.json` failures — 7-category signature taxonomy + flaky/persistent + coveredBy flow linkage) and diff risk-scoring (`--risk <baseline>`: 7 deterministic signals over the explorer's `MapDiff`, banded high/med/low, explainable reasons; failure history feeds the score). 34 new unit tests (297/297 total), typecheck/lint clean, phases-0-5 e2e gate run live: 4/4 no retries. Zero touches to existing behavior (wiring additive only). Design doc + decision log delivered; **exit gate CLOSED same-day — Jorge reviewed and approved the decision log (2026-07-14), resolving D1 as a policy change (model routing neutralized to Fable 5 until Phase 9 completes; CLAUDE.md updated). Phase 7 is unblocked.** Remaining known gap: the risk CLI needs a caller-supplied baseline map (no automatic baseline snapshotting) — a deliberate v1 scope line, candidate for the CI diff-gate integration later.

**Phase 7 is done, v1 (2026-07-14, same day)** — the Selector Healing Agent, same working pattern as Phase 6 (autonomous, decision-log-audited, full cycle on Fable 5 per the neutralized routing policy). New `healer/` sub-project (`pnpm heal`): reactive healing of `selector-drift` failures — parse the broken locator from the real error message, rank map candidates, live-probe proposals via `locate()` on DES. 30 new unit tests (327/327 total), phases-0-6 e2e gate 4/4 live, zero regressions. Live demo: a wording drift healed and **confirmed live**, and the verbatim A6 historical drift honestly yielding **no-candidates** (its fix was a flow change — human territory). One live iteration consumed (ranking-noise root cause: containment/token length floors — decision log D12). **Exit gate CLOSED same-day — Jorge reviewed and approved the decision log. Phase 8 (Continuous Learning) is unblocked.**

**Phase 8 is done, v1 (2026-07-14, same day)** — Continuous Learning, same working pattern. New `learning/` sub-project (`pnpm learn`): committed `coverage/run-history.json` (first real entry recorded live; the stale-risk-report freshness guard proved itself on its first run), consumed by analyzer (k-of-n failure-history reasons, live-demonstrated) and planner (drift-aware ranking, visible reordering live-demonstrated). 20 new unit tests (347/347), phases-0-7 e2e 4/4 live, zero regressions, 1 of 8 iterations consumed (a test-fixture arithmetic slip, not implementation). **Exit gate CLOSED same-day — Jorge reviewed and approved the decision log. Phase 9 is unblocked.**

**Phase 9 v1 (orchestration core) is done (2026-07-14, same day)** — same working pattern. New `orchestrator/` sub-project (`pnpm qa-cycle`): deterministic five-step pipeline with fixed per-step failure policy, consolidated report with the artifact-freshness rule (D5, the Phase-8 lesson enforced at the reporting layer), strictly propose/report (D4: `plan --update` opt-in only). 16 new unit tests (363/363), 1 of 8 iterations consumed (a lint nit). Live-validated: one real `pnpm qa-cycle` against DES, all five steps green — suite 4/4 (178s), history grew to 2 real entries, nothing-to-heal path exercised, 160 proposals ranked. **Exit gate: Jorge's decision-log review pending. B-NL1 is the next registered milestone (own scope round).**

**B-NL1 is done, v1 (2026-07-14, same day) and its gate is CLOSED — the roadmap's final item.** `pnpm ask` (new `intent/` sub-project): deterministic NL→flow resolver, generation via `selectJourneyByFlowId` (the §E injection point, checkout guard intact), auto-pick/ambiguity-list/honest-no-match conversation shapes all live-validated + one `--run` 5/5 green. Jorge approved the decision log same-day. **The platform is in use-and-maintain mode: no phase, milestone or gate remains open.** Maintenance candidates: F8 (⚠), D15, C11, CI wiring.

**Maintenance sweep done (2026-07-14, same session as the roadmap's completion):** **F8** (act→verify→retry centralized into `src/support/retry.ts`; the mandated live re-validation caught and fixed a latent M1-era disabled-size-button bug), **D15 phase 1** (the real checkout reached for the first time — `/es/checkout.html` via "Tramitar pedido"; permanent `tests/checkout/checkout-reach.spec.ts`; B13's path-hint validated; findings §22), **CI migrated to GitHub Actions** two-tier (`.gitlab-ci.yml` retired; `ci.yml` cloud gates already green; `qa-cycle.yml`/`explore.yml` await the self-hosted runner — `docs/ci/github-selfhosted-runner.md`), **F10** and **F3** closed earlier the same day. Dead code swept (`Frontier.visitedCount`); README rewritten for the full platform. **What remains:** C11's one manual step (Jorge installs the runner; first green `qa-cycle` closes it), and optional depth work (D15 phase 2, `ask` v1.1, CI `--risk` wiring, C13-CI) — each needs its own scope round. See CLAUDE.md's "Pending tasks for next session" and the backlog's resume section.

**C11 is done (2026-07-17), deepened 2026-07-18** — the GitHub self-hosted runner installed on Jorge's machine (label `des-vpn`) + the 3 repo secrets; first green live `qa-cycle` from Actions (run 29617369558, all steps success), closing backlog §C. Next day the runner was promoted to a **Windows service** (`NETWORK SERVICE`, delayed auto-start — survives reboots; scheduled L-V 07:00 jobs run unattended while the machine is on and the VPN connected). The switch surfaced and fixed two service-account gaps in the live workflows (bash probe → PowerShell, `366175a`; empty Playwright cache hitting the corp-proxy cert, TLS relaxed for that step only, `a9bd57d`), verified green end-to-end (run 29638531264). `docs/ci/github-selfhosted-runner.md`.

**D15 phase 2 is mid-branch and PAUSED (2026-07-18/19)** — scope round, design and plan done with Jorge on 2026-07-18 (full package, strict read-only; probe-first ladder), then executed subagent-driven in worktree `.claude/worktrees/d15-phase2-checkout-inner` through Task 4.5: the Task-1 live probe produced findings §23 and **branch decision C** (`/es/checkout.html` is server-routable), `seedCheckout`/`primeCart`/the checkout crawl seed shipped on the branch, plus an inserted Jorge-authorized Task 4.5 (LoginPage dual-variant fix — the "Continuar con e-mail" interstitial returned; findings §19 reopened and re-closed, setup PASS live). Task 5's permanent checkout-structure spec is written (uncommitted) with its signals live-verified, but its walk-in hit a sustained `/es/q/` dead-load outage (4/4 attempts across two calendar days) — **Jorge chose to pause the branch (2026-07-19)**. Task 6 + final whole-branch review + merge remained at that point. **→ Completed 2026-07-21, see the entry below; the worktree and branch no longer exist.**

**`pnpm ask` v1.1 session-twin grouping is done (2026-07-19)** — executed same-day from design to merge (`dfbd271`; three task-reviewed commits + a clean final whole-branch review, subagent-driven): `IntentMatch.session`, the pure `intent/group.ts` grouping module (exactly-2-differing-session rule, `JSON.stringify` bucket keys, order preservation), and grouped display at `intent/cli.ts`'s two list call sites. Display-only by design — scoring, tie-breaks, auto-pick, the `picked` path and the `--flow` hint untouched. Live-validated against the committed map: `"prueba el carrito"` collapses 4 qualifying flows into 2 grouped entries. **The LLM resolution seam remains the open half of the v1.1 candidate** (backlog §E).

**D15 phase 2 is done and merged (2026-07-21)** — the paused branch above was resumed and closed the same session (merge `28a080d`, pushed). The `/es/q/` outage that caused the pause was over; the health check (re-running the Task-5 spec) instead surfaced a different live drift — the checkout cost-summary disclosure button's accessible name is **state-dependent** (`"Ver detalle de costes"` collapsed vs `"Ocultar detalle de costes"` expanded), fixed by accepting both. Task 6's opted-in crawl took three attempts, stated plainly: one externally stopped, one where **DES had purged the shared cart server-side** so `primeCart` legitimately returned `failed` and the seed was skipped (diagnosed with a temporary probe, not blind re-runs), and a third that landed it after pre-priming the cart. Result: `/es/checkout.html` in the canonical map (auth session, `pageType: Checkout`, 5 elements matching findings §23's inventory), 1 Checkout flow, 154 pages/flows, B17 guard intact (4,245/4,245 unique element ids); `pnpm ask "checkout"` resolves both phrasings with zero `intent/` changes — the D15 blind-spot answer is retired by the map itself. **One design gap found at the gate and decided by Jorge:** the Builder's deliberate `CHECKOUT_ROUTE` path guard still refuses to *generate* a checkout draft (generated specs carry no `checkoutAllowed` skip — the guard is the protection); the guard stays, a safe version is a filed backlog item. Final whole-branch review: zero Critical. Findings §23's completion section.

**Roadmap and coverage gaps are now both closed — the project is in `use-and-maintain` mode (Jorge's decision, 2026-07-21).** There is no open milestone and no open gate. The default activity is running the platform against DES as real QA work (`pnpm qa-cycle`, `pnpm ask`, periodic `pnpm explore --update`) and letting accumulated run history decide what gets built next; the backlog is a pull-based menu, not a queue. See CLAUDE.md's "Operating mode" section.

**Desktop-layout migration (2026-07-29 → 2026-07-30, COMPLETED — commit `b685905`).** Found mid-onboarding (2026-07-29): the whole suite and the canonical map had been testing DES's **MOBILE** layout since inception — the team's manual QA tests desktop, which needs `?device=desktop` per navigation (server-side, no cookie persists). Session 2026-07-29 shipped the framework fix (`BasePage.goto()` forces the param, `8908df3`), fixed the one real layout casualty (`bombacho-barrel.spec` — testId uniqueness is layout-dependent, B16's lesson with a twist), got the manual suite 7/7 on desktop, and prepared the crawler (`EXPLORER_DEVICE` default desktop `dce25b5`, 5s interaction-click bound + shared PDP ledger scope `f1960b5`, heartbeat/phase logs `c58fce7`/`8ada575`) after a first re-crawl attempt hung silently 70+ min. Session 2026-07-30 landed the re-crawl: **the canonical map is now desktop-layout knowledge** — 140 pages/140 flows, 0 errors, 42 PDP/53 PLP (best PDP population ever), B17 clean (4,291/4,291 unique ids), must-capture Tallas overlay captured, 0 mobile-drawer elements. The 29th's hang did not recur; the 5s bound was seen defusing its suspected cause live ("Pausar video" skips). Downstream all green: suite 7/7, `plan --update` **10/140 covered (best ever)**, Builder pruned mobile-era drafts + 5/5 generated live. Full detail: findings §24 completion note. **Operational lesson:** long crawls must launch detached (`Start-Process`) — the agent-harness kills background tool tasks at ~40 min (the first 2026-07-30 attempt died that way at 114 pages, map untouched by design). **Two open items handed to the backlog:** the **seeded re-crawl** (the crawl ran without `EXPLORER_SEED_CHECKOUT=on`, so the desktop map has no Checkout flow and `pnpm ask "checkout"` no-matches — backlog P0, the priority pending item), and the desktop PLP filter/sort viewport gap (P4). **P0 closed the same day** — see the next entry.

**Seeded desktop re-crawl — P0 CLOSED (2026-07-30, later; map commit `3b901bf`).** One clean attempt, launched detached per the lesson above: 139 pages (52 anon + 87 auth), `primeCart` succeeded first try, `/es/checkout.html` crawled as the auth session's first page. All gates verified against the JSON: `pageType: Checkout` present, 1 Checkout flow, 3,781/3,781 unique element ids, must-capture Tallas satisfied, desktop fingerprint intact. `pnpm ask "checkout"` resolves again (generation still refused by the deliberate `CHECKOUT_ROUTE` guard — expected). One observation recorded, not chased: the map's desktop checkout *element inventory* looks like an abandon-confirmation dialog rather than the shipping chooser — resolved later by §24's dual-layout table as a capture artifact; `checkout-structure.spec` remains the source of truth for checkout's inner structure. The backlog queue is empty from here on: everything remaining is pull-based menu. Findings §24's seeded note.

**Framework-defect run (2026-08-04) — three real bugs, all found by reading evidence instead of re-running.** Three sessions' worth of work landed the same day: the PDP wishlist toggle shipped as a POM pair with a repeated-element strict-mode bug root-caused and fixed (findings §25); the first deliberate **coverage expansion by promoting Builder drafts** — suite 8 → 14, including a Builder-inherited unbounded-click defect found and filed as backlog P4 (findings §26); the Footer component object, with `pnpm ask` correctly answering no-match for it (chrome is not a flow — findings §27); and the closure of the long-running "add-confirmation drawer" watch item, which turned out **never to have been a DES problem**: the detector counted dialogs instead of identifying one, so it could not see a drawer that was on screen (findings §28). Method note that earned its place: `test-results/` is overwritten by the next run, so the failure's own `error-context.md` must be read *before* re-running.

**False-green discovery and coverage round 2 (2026-08-06).** Onboarding Fase 6 injected a selector defect to practise diagnosis and instead exposed that the wishlist spec was **structurally incapable of failing** — it asserted a state, not a transition, on a shared account that carries wishlist items across runs; fixed by establishing the starting state and asserting the whole transition (findings §29). Platform consequence worth remembering: **a false green is invisible to `analyze` and `heal`** — no failure, no classification, no healing proposal. Same day, coverage expansion round 2 promoted 10 more Builder drafts: suite **15 → 25 tests**, coverage **26 → 38/139 flows**, the best in the map's history (findings §30). Its own method lesson: `pnpm plan --update` reflects only the *most recent* `pnpm test` invocation's evidence, so always run the full suite before updating the plan. This session also split the findings doc — §1 and §8–§22 moved to a non-auto-loaded archive to stay inside the 150k-char budget; §N anchors are load-bearing (hundreds of citations across code, specs and plans) and must never be renumbered.

**Locator anchoring — §29's last lead closed (2026-08-10).** Onboarding Fase 7 took §29's unverified suspicion to a proven defect and a validated fix: the wishlist `.first()` was never anchored to the main product, so `isInWishlist()` answered *"is ANY product on this page in the wishlist"* (a desktop PDP renders 43 matching buttons — 1 main + 42 cross-selling). Proven with a discriminating-state probe (main product out, one cross-selling card in → `true`), fixed by scoping both locators to the main product's panel and removing `.first()` and an ambiguity-hiding `.catch`, then validated as a controlled experiment (same probe, one variable changed, answer flipped). Findings §31. Durable rule: **`.first()` fixes ambiguity, never anchoring** — it switches off strict mode, which is the only ambiguity detector the framework gets for free.

**The cart's inside, probed and covered (2026-08-13).** `/es/shop-cart.html` had 0 elements in the functional map — the crawler never had a reason to interact past it, so no remove/quantity/total selector had ever been confirmed. A live probe established the structure (the cart line is a plain CSS-classed div, not a `listitem`; the remove control renames itself by quantity; the header tab counts UNITS, not lines) and the effort shipped `CartPage`, the `cleanCart`/`ensureEmptyCart` fixture — closing §7's long-standing cart-cleanup lead — the `cart-lifecycle` journey spec, and session-invalidation recovery (backlog P5). Four defects were fixed: three in its own new cart code, caught pre-merge by its own live gates, and **one genuinely pre-existing production defect** — `ProductPage.addToCart`'s page-wide-unanchored add button could add a *different* product, invisible to the whole suite because no spec had ever counted cart lines. §29's "a false green is invisible to the entire agentic stack" thesis, confirmed in production code a third time. Suite 25 → 26. Detail: findings §32.

**Backlog P6 root-caused and closed — it was never a DES defect (2026-08-16).** The "cold cart navigation renders member-hub with a valid session" lead, carried for three sessions with two competing hypotheses about DES, turned out to be our own code. Measured live: DES serves its server-rendered **logged-out** header for the first ~5-8s of a cold navigation on a perfectly valid session, and `CartPage`'s session detector read that single instant as a dead session — firing a re-login that then bounced the already-authenticated user to member-hub while `actUntil` swallowed the resulting error by design. Both standing hypotheses were falsified (one offline, from `.auth/state.json`; one live, from the navigation's own 200-with-no-redirect). Fix: the tell must **persist** before it is believed. Validated in both directions — the reproducer went 2/2 red to 2/2 green, and legitimate recovery still fires and lands on the cart. The same session archived a second findings tranche (§5, §23, §25, §30) to make room. Detail: findings §33.

**`/code-review high` plus two Jorge-directed follow-ups closed five more real bugs, same day as §33 (2026-08-16).** Requested specifically framed as "hunt real bugs, stay in use-and-maintain" — 8 finder agents + independent verification on the cart-regression diff (`e2272a9..HEAD`). Fixed: `ensureEmptyCart`'s session-blind-spot mid-drain (`0a9b8ca`, and its own live-validation step caught a regression the fix itself introduced — corrected before commit); the Builder's unbounded-click watch item, open since §26 (`03aa765`); half of a claimed analyzer vocabulary gap, the message CLAUDE.md's summary paired with it turned out already correctly classified (`368d334`). Then, working down the review's own severity-ranked list at Jorge's direction: the desktop PLP filter/sort blind spot needed **two** fixes, not the one first reported as closing it — a missing `complementary` overlay role (`c8b5544`), then, found only after Jorge asked for a live re-crawl to prove it in the map and that re-crawl showed 0/8 successes, a missing retry-on-a-throwing-click in the crawler's own `discoverInteractions()` (`01dd927` — DES's filter toolbar takes up to ~16s to even appear in the DOM). Reproducing that re-crawl's other silent failure (`primeCart failed — skipping the checkout seed`) surfaced a second bug of its own: no exception at all, just a bare unretried cart-tab read racing its own hydration (`9a9aade`). **All five fixes are live-validated in isolation; none has been proven together in one committed re-crawl** — two seeded re-crawls ran today (85 pages, then 192 pages) but both predate the two explorer fixes, so that proof is the standing next action, not a finished result. Full mechanism and evidence: findings §34.

**The standing re-crawl, run — and it split the five fixes into proven and unproven (2026-08-17).** The seeded crawl §34 left as the standing next action: full recipe, detached launch, 140 pages, exit 0, all B17 gates green, map committed `0144363`. **Proven in a real crawl:** `9a9aade` (checkout seeded on the first try — both of the previous day's crawls had failed exactly there) and `01dd927` (the 19 PLP filter/sort candidates exist in the map at all for the first time; interactions 66 → 119, elements 3781 → 3896). **Not proven, and re-opened:** `c8b5544`'s `complementary` overlay role — zero such elements, all 19 filter/sort interactions `outcome: 'none'`. The crawl also found a defect *inside* one of the fixes it was verifying: `01dd927`'s click-retry `catch` was silent, so the 8 `TimeoutError` lines §34's own diagnosis had been built on stopped being logged, and `none` could no longer distinguish "the locator never resolved" from "the click landed and revealed nothing" — the two branches that need opposite fixes. Fixed same session (`d5f9595`), which makes the *next* crawl's log the free instrument that settles it. Method note carried forward: two verification passes reported false gate failures because the script assumed the wrong map schema — read a sample object before trusting an aggregate. Detail: findings §35.

See [`2026-07-02-backlog.md`](./2026-07-02-backlog.md) for the live, prioritized pending list (its own resume section) and the full item catalog.

---

## 1. The original roadmap (reconstructed, and continued — not replaced)

The foundation spec (§11) states the intended sequence explicitly:

> **Phase 0 (Foundation)** → **Explorer Agent** → **Coverage + Test Generator** → **Builder Engine** → **Failure Analyzer + Selector Healing** → **Reporting/Dashboard** — coverage measured by user journeys / business processes, never by code lines or test counts.

That sequence maps 1:1 onto the 10-phase agentic evolution below. No redesign is needed; this document continues the existing plan.

| Phase | Capability | Original sub-project | Status (2026-07-02, updated) |
|---|---|---|---|
| 0 | Professional automation framework | Phase 0 foundation (POM/COM, fixtures, multi-env config, CI) | ✅ Done, live-validated against DES; interaction reliability hardened (act→verify→retry, M1) |
| 1 | Application discovery | Explorer Agent | ✅ **Closed**, deepened by M8. Aria-tree extraction (M2) + first live crawl (M2c): 150 unique pages, 2414 real elements, 0 errors. M8 adds interaction-based discovery: the crawler now opens overlays/dialogs, capturing knowledge (e.g. the PDP size selector) invisible to passive crawling |
| 2 | Persistent functional knowledge | Versioned `coverage/functional-map.json` + differ | ✅ Canonical map committed and diffable (schema 1.1, multi-step flows); CI diff gate wired (M3, scheduled/non-blocking) |
| 3 | Knowledge Graph | Evolution of the map schema — pages/components/elements/flows are already relational via stable IDs | ✅ Coverage annotations landed (M5b, schema 1.2): flows now carry `coveredBy` from real run evidence — the first run-result→map linkage (seed of Phase 8). Plain JSON + stable IDs suffices through Phase 4; no graph DB before it earns its keep |
| 4 | Planning Agent | Coverage + Test Generator agents | ✅ **Implemented and live-validated (M5b).** `pnpm plan`: evidence-based `coveredBy` + ranked proposals. Caveat (structural, still true): proposal quality is bounded by map completeness. The findings-§9 PLP-grid gap that originally caused it **closed in B12**; current coverage is 38/139 flows on the 139-page desktop map |
| 5 | Execution Agent | Builder Engine | ✅ **v1 implemented and live-validated (M6b, hardened M7, B14).** `pnpm build-tests` generates navigation specs + minimal page objects imitating BasePage/locate(); real, page-specific loaded-signals now win in every case — testId first (M7), and page-specific role/label over shared chrome as fallback (B14), now also excluding M8's interaction-revealed elements. **Interaction-spec generation landed in M9 (2026-07-06)** — `TemplateGenerator.generateInteraction` consumes M8's `interactions[]`; the template's unbounded click is a known open defect (backlog P4, findings §26) |
| 6 | Risk Analysis Agent | Failure Analyzer (+ risk-scoring of map diffs) | ✅ **v1 implemented (2026-07-14).** `pnpm analyze` (new `analyzer/` sub-project): the Failure Analyzer consumes `reports/results.json` (the day-one seam, first consumer ever) and classifies failures into a 7-category taxonomy anchored to real observed signatures, with flaky/persistent from retry data and coveredBy-linkage to map flows; `--risk <baseline>` scores map diffs (7 deterministic signals, explainable reasons). Design: `docs/superpowers/specs/2026-07-14-phase6-risk-analysis-design.md`; decision log: `docs/superpowers/notes/2026-07-14-phase6-decision-log.md` |
| 7 | Self-Healing | Selector Healing Agent | ✅ **v1 implemented (2026-07-14).** `pnpm heal` (new `healer/` sub-project): reactive, propose-don't-apply, mandatory live validation. Parses broken locators out of `selector-drift` failure messages, ranks map candidates (label/regex/token evidence + role agreement, chrome/overlay penalties, B16/B17 uniqueness via `count`, min-score floor), probes proposals live via the framework's own `locate()`. Live-validated both ways: a real wording drift healed and confirmed on DES, and the verbatim A6 historical drift correctly yielding no hallucinated candidates. Design: `docs/superpowers/specs/2026-07-14-phase7-selector-healing-design.md`; decision log: `docs/superpowers/notes/2026-07-14-phase7-decision-log.md` |
| 8 | Continuous Learning | Feed run results and diffs back into the functional map | ✅ **v1 implemented (2026-07-14).** `pnpm learn` (new `learning/` sub-project): cross-run memory in the committed `coverage/run-history.json` (stability events per spec/flow + drift per scored diff; idempotent, compacted, anti-clobber). Consumed by the analyzer (multi-run k-of-n failure-history reasons) and the planner (drift-aware proposal ranking, `driftEvents`). Loop demonstrated live: run → learn → visibly better decisions. Design: `docs/superpowers/specs/2026-07-14-phase8-continuous-learning-design.md`; decision log: `docs/superpowers/notes/2026-07-14-phase8-decision-log.md` |
| 9 | Autonomous Quality Engineering | Orchestration of phases 4–8 | ✅ **v1 orchestration core implemented (2026-07-14).** `pnpm qa-cycle` (new `orchestrator/` sub-project): deterministic five-step pipeline (test → analyze [--risk] → learn → heal → plan), per-step failure policy fixed at design time (a red suite is data, not a pipeline failure), consolidated `qa-cycle-report.json` with artifact-freshness guards, strictly propose/report (map annotation opt-in via `--update-map`). Live-validated: one real cycle, all five steps green. **B-NL1 (the NL interface) shipped the same day** — see the position note below; the roadmap has no registered work left. Design: `docs/superpowers/specs/2026-07-14-phase9-orchestration-design.md` |

**Current position: Phase 9 COMPLETE — the roadmap has no registered work left.** All ten phases have live-validated running code, including both Phase 9 halves: the orchestration core (`pnpm qa-cycle`) and the B-NL1 NL interface (`pnpm ask`, done 2026-07-14 — deterministic resolver over the map's flows, generation via the `selectJourneyByFlowId` injection point, honest D15 blind-spot answers). The platform now runs its full loop unattended AND takes targeted human instructions in natural language, with every mutation of committed code/knowledge still gated by a human. What remains open is maintenance/deepening work only — no phase, no registered milestone; the live prioritized list is the backlog doc's resume section (deliberately not copied here — any copy goes stale by construction).

*(Previous position note, kept for history:)* **Phase 5 reached (v1, navigation-only), hardened.** The Explorer has real, verified, multi-step knowledge of DES; the Coverage Planner closes the loop deterministically; the Builder Engine turns proposals into live-passing generated specs whose assertions now rest on trustworthy, page-specific selectors in every case — testId first (M7), and page-specific role/label over shared chrome as fallback (B14). The chain from discovery → planning → generation → resolution is proven end-to-end for navigation journeys, with the selector-resolution layer itself hardened for whatever consumes it next (including the future Selector Healing agent, Phase 7). See "Where a fresh session resumes" above for what's next.

---

## 2. How each existing module evolves toward the platform

> **Written 2026-07-02 as design intent. Every item below shipped** (phases 0–9 all closed 2026-07-14). Kept for the *reasoning* — why each seam exists — not as a plan. Future tenses below are historical.

- **Explorer → the Knowledge Engine.** Near-term: accessibility-tree-driven extraction (works where `page.content()` cannot), onboarding-tour suppression in the crawler, an `errors[]` artifact section, first committed canonical map. Mid-term: multi-step flow synthesis (Home→Search→PLP→PDP→Cart, done M4), interaction-based discovery (open nav menus/overlays, done M8), per-page state capture for later agents.
- **Functional map → the Knowledge Graph.** Schema v1.0 is already relational (stable IDs cross-referencing pages/components/elements/flows). Evolve additively with `schemaVersion` bump discipline: richer flows, coverage annotations (`coveredBy: [spec ids]`), risk/priority signals derived from diffs.
- **POM/COM + fixtures → the Execution Engine's target language.** The Builder Engine will generate code imitating `BasePage`/`BaseComponent` and the `locate()` strategy. Keep the contracts boring and uniform — every irregularity added now is a special case a generator must learn later. Long-term (Phase 9, B-NL1): a natural-language instruction layer will resolve user intent against `MapFlow.name`/`type` and inject a specific flowId at the Builder's `selectJourneys()` selection point, bypassing (not replacing) the ranking for that one request.
- **`src/support/locators.ts` (`Strategy`) → the Self-Healing seam.** Healing = swapping strategies when selectors break, informed by the map's `selectorHints` and testId-presence data.
- **JSON reporter output (`reports/results.json`) → the Failure Analyzer / Reporting seam.** Deliberately emitted since day one; **consumed since Phase 6 (2026-07-14)** by `pnpm analyze` and, through it, by `pnpm learn`'s run history. What is still unconsumed is the *CI-side* stage — backlog C13/P3.
- **Config** grows per-agent sections following the `loadExplorerConfig` pattern (defaults + env + explicit overrides, fail-fast validation).
- **CI/CD** gains stages progressively: Explorer diff gate (`explore --diff --fail-on-new`, Phase 2), generated-test execution (Phase 5), risk reports (Phase 6).
- **Docs** keep the working `docs/superpowers/{specs,plans,notes}` cycle per sub-project; this `docs/roadmap/` area holds the platform-level view.

---

## 3. Milestone sequence (all closed — chronological record)

| Milestone | Content | Phase gate | North Star |
|---|---|---|---|
| **M0** ✅ | Platform roadmap + backlog committed as versioned docs | — | Knowledge, Engineering Excellence |
| **M1** ✅ | Stabilized `search-plp-pdp.spec` / `add-to-cart.spec`: act→verify→retry on every state-changing DES interaction, `workers: 1`, `retries: 1` | Reliably green suite = precondition for everything agentic | Engineering Excellence |
| **M2** ✅ | Explorer DES-readiness: aria-tree extraction, crawler tour suppression + time budget, `errors[]` artifact; **first live crawl** → committed `coverage/functional-map.json` (150 pages, 2414 elements) | Closed Phase 1, opened Phase 2 | Knowledge |
| **M3** ✅ | CI Explorer gate: `explore` stage in `.gitlab-ci.yml` running `pnpm explore --diff --fail-on-new` — **scheduled-only, non-blocking** (`allow_failure: true`) by deliberate choice, since the map is still bounded/incomplete (B8 flow synthesis, PLP-grid gap). **Superseded 2026-07-14** — CI migrated to GitHub Actions (`.gitlab-ci.yml` retired); the schedule now lives in `.github/workflows/explore.yml` on the self-hosted runner, and the runner-reachability question (C11) closed 2026-07-17. The GitLab schedule step below is moot, kept as history: *a GitLab CI/CD Schedule must be created (Settings → CI/CD → Schedules) for the job to ever run* | New-flow detection runs without a human, once scheduled | Autonomy |
| **M4** ✅ | Flow synthesis: `MapFlow.steps` now carries real root→leaf navigation chains reconstructed from `discoveredVia` (schema 1.1; canonical map refreshed live: 152 flows, 74 multi-step). Coverage annotations deliberately deferred to M5, where they belong | Phase 2 → 3 | Knowledge |
| **M5** ✅ | Coverage Planner (Planning Agent): `pnpm plan` annotates flows with evidence-based `coveredBy` (schema 1.2) and ranks uncovered flows into proposals. Live-validated 2026-07-03: 3/152 flows covered by the 3-spec suite; first annotated map committed. Coverage usefulness bounded by map completeness (findings §9) | Phase 4 | Reasoning |
| **M6/M6b** ✅ | Builder Engine (Execution Agent) v1: `pnpm build-tests` generates navigation specs + minimal page objects from the planner's proposals, imitating the POM/COM contracts. Live-validated 2026-07-03: 3/3 generated specs pass against DES. Surfaced and worked around a real testId/`locate()` gap (backlog B15) | Phase 5 | Autonomy |
| **M7** ✅ | TestId attribute-provenance fix (closes B15): `TestIdHint { attr, value }` records which of `data-testid`/`data-qa-anchor`/`data-qa` matched; `locate()` resolves each correctly; Builder's testId priority restored. Live-validated 2026-07-03: 2,508 elements now carry provenance, 3/3 regenerated specs pass using real page-specific testIds, full manual suite unaffected (no regression). Partially closes B14 | Phase 5, Phase 7 seam | Engineering Excellence |
| **M7b** ✅ | B13 classifier fix: deterministic path rules (PDP `-c0p{id}.html`, Cart `shop-cart.html`) before text signals; Checkout requires a path hint. Live-validated 2026-07-04: full re-crawl, all `-c0p` → PDP, zero fake Checkout labels, planner ranking decontaminated | Phase 2 knowledge quality | Knowledge |
| **B14** ✅ | Shared-element deprioritization: extraction paths tag Header/Footer/MiniCart component provenance on elements (schema 1.4); Builder's `loadedSignalFor` runs pass-major (page-specific candidates across all selector tiers before falling back to shared chrome). Closes the scope M7 left open. Live-validated 2026-07-04: full re-crawl (152 pages), 17/17 generated specs pass, including a no-testId leaf page that now asserts a page-specific signal instead of generic header chrome | Phase 5 signal quality | Engineering Excellence |
| **M8** ✅ | Interaction-aware crawl: the crawler opens a bounded, deduped set of overlays/dialogs during the crawl (`explorer/crawl/interact.ts`); schema 1.4→1.5 adds `interactions[]` + `revealedBy`; Builder excludes revealed elements from loaded-signal selection. Closes backlog B9's "nav menus/overlays opened during crawl" row. Live-validated 2026-07-05: the "Añadir a la cesta → Tallas dialog" mechanism fired twice across two full crawls (size-selector knowledge invisible to passive crawling, §10), though not in the crawl chosen for the committed canonical map (M9 prerequisite); neither crawl's discovery order reached a literal PDP page (pre-existing crawl-order variability, §9/§12/§13, not an M8 defect). `pnpm test` 4/4, `pnpm test:generated` 20/20 | Phase 1/2 knowledge depth | Knowledge |
| **M8b** ✅ | Deterministic must-capture interactions: `interactions.mustCapture` config gives matching trigger labels priority in a two-pass `selectCandidates`, retried across pages until satisfied once per crawl (`InteractionLedger` equivalence-class + satisfaction bookkeeping); category-PLPs collapsed into one ledger scope; ledger made per-crawl-global. No schema change. Closes the M9 prerequisite left open by M8. Live-validated 2026-07-05: a 32-page probe and the full 149-page re-crawl both captured "Añadir a la cesta" → Tallas; the **committed canonical map now contains the capture**, verified directly. Surfaced two new, unrelated, out-of-scope findings (A5/B16) during no-regression checks | Phase 1/2 knowledge depth, Phase 5 seam for M9 | Knowledge, Autonomy |
| **M9** ✅ | Builder interaction-spec generation: `selectInteractionJourneys`/`unsatisfiedMustCapture` (map-only, shares must-capture config with the crawler) + `TemplateGenerator.generateInteraction` (open→verify→close, act→verify→retry) + CLI wiring. Same milestone closes **B16** (non-unique testId excluded from loaded-signal selection, deprioritize not exclude). Live-validated 2026-07-06: found and fixed a genuine live bug (persistent DES chrome dialog broke a bare `getByRole('dialog')` overlay-open signal; fixed with a baseline dialog-count diff mirroring M8's own crawler idiom). After the fix: `pnpm test:generated` 5/5 no retries (interaction spec opens/verifies/closes Tallas live), B16 confirmed fixed live, `pnpm test` 3/4 (A5 confirmed unrelated) | Phase 5 (Builder consumes interaction knowledge), Engineering Excellence (B16) | Autonomy, Engineering Excellence |

Rules of engagement (unchanged from the repo's working method):

1. Each sub-project gets its own **spec → plan → implement** cycle under `docs/superpowers/`.
2. Never implement everything at once; milestones stay small and independently verifiable.
3. Anything requiring live DES access is validated live (see `docs/superpowers/notes/`) before being declared done.
4. No shortcut that makes a later agentic capability harder to implement (e.g., irregular POM contracts, non-deterministic IDs, hardcoded URLs).
